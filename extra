{% extends 'layout.html' %}

{% block body %}
<div class="container mt-5">
    <!-- Upload CSV Section -->
    <div class="card mb-4 shadow-sm">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">Optimize Warehouse Allocation</h4>
        </div>
        <div class="card-body">
            <form method="POST" enctype="multipart/form-data" id="inventoryUploadForm">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <div class="input-group">
                            <input type="file" class="form-control" name="inventory_file" accept=".csv" required>
                            <button class="btn btn-success" type="submit">
                                <i class="bi bi-cloud-upload me-2"></i>Upload & Analyze
                            </button>
                        </div>
                        <small class="text-muted d-block mt-2">
                            Upload CSV with district-wise inventory data
                        </small>
                    </div>
                    <div class="col-md-4">
                        <div class="alert alert-info p-2 small" role="alert">
                            <i class="bi bi-info-circle me-2"></i>
                            Our AI will predict the most optimal warehouses for your inventory.
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    {% if show_prediction and warehouses %}
    <!-- Existing prediction results section remains the same as in the previous warehouses.html -->
    {% endif %}
</div>

<!-- Existing modal and scripts remain the same -->
{% endblock %}